#!/bin/csh -f

#
# Program: purl.obolibrary8.org

#
# Original Author: sc
#
# Purpose:
#
#	Download the  HPO obo file
#
# Modification History:
#
# 03/07/2016    sc
#       - new 
#

cd `dirname $0` && source ./Configuration

# Full URL of the file

setenv URL	"http://purl.obolibrary.org/obo/hp.obo purl.obolibrary8.org"

# Base URL of the file
setenv BASEURL purl.obolibrary.org

# full path to log file
setenv LOGFILE	${MIRRORLOG}/${BASEURL}8.log

# output directory
setenv OUTPUTDIR        ${DATADOWNLOADS}/${BASEURL}

# output file
setenv OUTPUTFILE       ${OUTPUTDIR}/hp.obo

#
# -k = insecure/SSL; allows curl to perform insecure SSL connections 
# -o = the output file
# --create-dirs = used with -o, create dirs that don't exist in output file path
#

curl --create-dirs -ko ${OUTPUTFILE} "${URL}" >& ${LOGFILE}
